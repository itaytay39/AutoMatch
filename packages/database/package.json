{
  "name": "@car-aggregator/database",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "migrate:dev": "prisma migrate dev",
    "migrate:deploy": "prisma migrate deploy",
    "generate": "prisma generate",
    "seed": "tsx seed.ts"
  },
  "dependencies": {
    "@prisma/client": "^5.14.0"
  },
  "devDependencies": {
    "prisma": "^5.14.0",
    "tsx": "^4.15.7"
  },
  "prisma": {
    "schema": "schema.prisma",
    "seed": "tsx seed.ts"
  }
}
